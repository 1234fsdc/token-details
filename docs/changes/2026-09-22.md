- 发布到 GitHub `1234fsdc/token-details`：仓库只保留 `main` 一个分支；新增 README（Releases 下载 exe + 源码运行说明）。
- 创建 Release v1.0.0，附件 `TokenDetails.exe`（37.9MB，commit 53c287c 清缓存重新 PyInstaller 打包，self-check 通过）；旧 exe 为缓存构建未更新代码，已清除 build/dist 后重建确认时间戳与体积变化。
- Release v1.0.0 追加 zip 打包（Compress-Archive）并附 SHA256；浏览器"文件不常见"拦截属 SmartScreen 信誉机制非病毒（本地 Defender MpCmdRun 扫描无毒），release 说明与 README 增加误报解释与"仍要保留"引导。
