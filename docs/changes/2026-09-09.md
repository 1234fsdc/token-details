- 修复运行中模型误显示：`message` 表中的 assistant 记录现在必须在 3 分钟活动窗口内，过期消息不再被标记为正在生成。
- 修复 gpt-5.6-new 误显示 0.0 t/s：Vyce 供应商完成行 usage 为空（0 token），`calc` 对 0-token 行改判"无数据"，不再顶掉正在生成的"运行中…"行。
- 修复供应商名称显示 UUID 前缀：`provider_names()` 原先只在面板启动时读一次，push_loop 改为每秒重读配置，面板运行期间新增/改名供应商立即生效。
