- self_check 补 data-JSON 模式夹具：live_model 三态断言（超窗残行/窗口内运行中/列式退回）+ `_last_act` 返回值与无 data 列退回 {}——生产真实 schema 路径首次有护栏（回顾问题 1）。
- build.bat 打包成功后写 `dist\build_info.txt`（时间戳 + git 短哈希），排障时一眼判断面板跑的是哪版；该文件在 dist/ 下本就被 gitignore（回顾问题 2）。
- changelog 整理：2026-09-10 条目从 2026-09-09.md 拆出到独立日文件（规则 7"次日新建"）。
- 新增显示快照日志 `C:/Users/Public/panel_trace.log`：面板行集合（模型/供应商/速度）每次变化记一行，超 1MB 清空——转瞬即逝的显示问题事后可回查（用户提出：之前只能靠数据库反推"应该显示什么"，无法知道"实际显示了什么"）。
- 全项目代码审查修复：page_tok 删除重复的 mbody/cn 死代码块；删除未使用的 `ctypes.wintypes` 导入与 `POLL_MS` 常量；`_parse_rollout` 文件句柄改 with；简略面板 JS 对模型名/供应商名做 HTML 转义（与详情页口径一致）；`latest_by_model` 窗口 30s→90s 与面板同步；速度页空态文案"3 分钟"改"90 秒"。
- 会话压缩保留期 3 天（按对话活跃门槛）→ 30 天（按压缩记录自身时间），超 30 天的压缩记录不再显示，口径改为"30 天内压缩 N 次"。
- 详情总览页新增"每模型速度"区块（与简略面板同源同 90s 窗口）。
- 总量页合并按模型/按会话切换下拉为同页两段展示，删除 tcat 轮询与 cur_cat 状态。
- 构建环境：.agent-reach-venv 重建后丢失 PyInstaller，已装回；本次起构建命令显式用该 venv 的 python。
- 修复 exe 启动报 "No module named 'webview'"：.agent-reach-venv 被重建后连 pywebview/pystray 一起丢了（此前只装回了 PyInstaller），导致打包出的 exe 缺模块；已装回两包并重打包。build.bat 增加 venv 回退（存在 %USERPROFILE%\.agent-reach-venv 则优先用），不再依赖失效的 PATH python。
