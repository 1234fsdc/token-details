- 悬浮面板改为真正透明背景：移除原先 `#fffdf8` 淡黄色底，HTML/body 显式使用 `transparent`，并将分隔线与辅助文字改为中性半透明灰；详情窗口保持原有不透明样式不变。
- 透明合成采用兼容 WebView2 的 Win32 分层 alpha：默认 alpha 从 150 调低到 125，移除颜色键方案；模型名/速度数字加深、加粗并加轻微高光描边，降低背景透明度时保持可读，不产生黑底。
- 详情模型行增加 `cnt/rpm` 缺省值容错：90 秒窗口无今日模型时不再因回退到实时模型数据而触发 `KeyError: 'cnt'`，避免推送循环异常影响面板刷新。
