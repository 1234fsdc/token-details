## 2026-09-10

- 修复 glm-5.3-flash 速度行"运行中→消失→再出现"：该模型多数完成行缺 `first_token_at`，v4 起缺首字行被整段舍弃导致模型时隐时现；`avg_by_model` 加 duration 回退（标 `*`，含排队会虚低），不再消失。
- 面板布局：拖拽把手 ⠿ 从左上角移到首行模型左侧，上下留白对称（顶部 padding 0 / 底部 10px）。
- 活动窗口 3 分钟 → 30 秒（用户改定）：运行中和速度行 30 秒无活动即消失。
- 速度行判活改为"最后任何活动时间"（含生成中的 assistant 消息）：修长回合（glm-5.3-flash 40s+）落库时已超窗口导致速度永远不显示、只见"运行中"的问题。
- 判活窗口 30s → 120s（用户改定）：30s 在回合间隙（思考/工具执行）闪没行。取舍：停止后行也多留 2 分钟。
